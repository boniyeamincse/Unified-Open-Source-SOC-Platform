####################################################################
#  Unified Open-Source SOC Platform
#  Author : Boni Yeamin
#  File   : scripts/requirements.txt
#  Purpose: Python dependencies for SOC automation scripts.
#           All Phase 1-3 scripts use only stdlib, so this file
#           lists optional runtime dependencies for future use.
####################################################################

# ── Core scripts (audit-logger.py, risk-scoring.py, alert-dedup.py,
#    anomaly-detector.py, forensic-export.py, compliance-report.py)
#    use ONLY Python 3.10+ standard library — no pip installs needed.

# ── Optional: Wazuh-MISP Integration ──
pymisp>=2.4.178
requests>=2.31.0

# ── Optional: Prometheus client for custom exporters ──
prometheus-client>=0.20.0

# ── Optional: OpenSearch client for direct querying ──
opensearch-py>=2.4.0

# ── Optional: Slack/email notifications ──
slack-sdk>=3.27.0

# ── Development / Testing ──
pyyaml>=6.0.1
pytest>=8.0.0
ruff>=0.2.0
